# v1.2.0 변경사항

## 1·2·3학년 공용화
- 상단 문구를 `3학년 수시상담 · 빠른입력 모드`에서 `1·2·3학년 수시상담 · 내신 통합분석`으로 변경
- 프로필 바에 `현재 학년` 선택 추가
- 현재 학년과 학년도를 기준으로 입학연도를 역산하여 5등급제/9등급제를 자동 판정
- 2026학년도 기준 고1·고2는 5등급제, 고3은 9등급제로 자동 기본값 설정
- 학생의 현재 학년에 맞춰 자동추출 성적의 실제 학년도와 코호트 정책을 계산

## 5등급 → 9등급 참고 환산
- 사용자 제공 `내신 목표 등급 설정(1).xlsm`의 `9등급 환산` 시트 A2:B73을 코드에 그대로 반영
- Sheet1!J2의 `IFERROR(VLOOKUP(I2,'9등급 환산'!A2:B73,2,1),"")`와 같은 근사검색 동작 구현
- 5등급 평균 원값은 보존하고 9등급 환산값을 참고값으로만 병기
- 9등급제 데이터는 9등급 환산 열에 원값을 그대로 표시
- 등급체계 혼재 시 기존 원칙대로 공식 평균 계산을 차단

### 9등급 환산 표시 위치
- 개별 성적 입력행
- PDF/이미지 자동추출 검토표
- 교과군별 가중평균
- 국영수 / 국영수사 / 국영수과 / 국영수사과 / 전교과
- 1-1 / 1-2 / 2-1 / 2-2 / 3-1 / 전체 학기별 조합표
- 목표등급 역산 및 시나리오 표
- Excel 01_상담요약 / 02_성적원자료 / 03_성적분석

## 성적 이미지 다중 처리
- 파일 선택 시 PDF/이미지 여러 개 동시 선택 가능
- 드래그앤드롭 시 여러 파일 동시 처리
- Ctrl+V로 캡처 이미지를 여러 번 붙여넣어도 앞선 검토 후보를 유지하고 누적
- 같은 성적 후보는 중복 키로 자동 제외
- `검토 후보 비우기` 버튼 추가
- 여러 파일 처리 진행상태를 `[현재/전체] 파일명 · 단계 · 진행률`로 표시

## 계산 원칙
- 가중평균은 계속 `Σ(단위수×등급) ÷ Σ단위수`
- 전체는 학기 평균의 단순평균이 아니라 원과목 전체 단위수 가중평균
- 성취평가 A~E는 석차등급 평균에서 제외
- 5→9 환산은 상담용 참고값이며 대학별 공식 환산식이 아님

## 검증
- 신규 5→9 환산 테스트 5건 추가
- 전체 테스트 정의 87건
- 수정된 UI/계산엔진 전체 TypeScript 정적검사 통과(ExcelJS 의존부는 타입 스텁으로 별도 검사)
